# Smart Metro 검토 결과 — 2026-09-09

## 판정

발견한 핵심 계산·알람·설정 오류는 수정했다. 그러나 현재 결과물을 기획 전체 구현 완료 또는 실서비스 출시 가능 상태로 판정하지 않는다. 원본 기능 기획서는 작업 폴더에서 찾지 못했고, 운영 인프라 및 실기기 확인도 남아 있다. 이번 변경은 로컬 작업 폴더에 있으며 GitHub 업로드와 Vercel 재배포는 수행하지 않았다.

## 검토 기준

- `stitch_smart_bus_commute_alarm/transit_flow/DESIGN.md`
- 같은 폴더의 홈, 노선 등록, 반복 일정, 알림 스타일 화면 시안 4개
- README.md, DEPLOYMENT.md, mobile_app/README.md
- 이 대화에서 요청한 실시간 버스, 반복 알람, 한국어 음성, 지도, 계정 기능
- 웹 상태/계산/알림 코드, 서버 공급자·계정·예약·전달 코드, Flutter 설정·로컬 예약·로그아웃 흐름

원본 기능 기획서가 제공되면 화면 시안만으로 확정할 수 없는 조건과 우선순위를 다시 대조해야 한다.

## 수정한 오류

1. 선택한 버스가 조회 결과에 없으면 다른 노선의 첫 번째 버스를 대신 쓰던 동작을 제거했다.
2. 버스 한 대의 도착정보만 있는데 두 번째 버스 시간을 임의로 만드는 동작을 제거했다. 미수신 정보는 '정보 없음'으로 표시한다.
3. 오래된 실시간 시간을 가상의 배차 간격으로 반복시키지 않는다. 최초 조회 시각에서 남은 시간을 차감하고 2분을 초과한 자료는 사용하지 않는다. 2분은 이번 변경의 보수적인 유효시간 기준이며 운영 측정 후 조정할 수 있다.
4. 실시간 정보가 끊기면 예시 버스로 정시·지각 판정을 만들지 않는다. 도착정보 확인이 필요하다는 안내를 제공한다.
5. 실제 등록 노선과 정류장을 알람 계획에 사용하며, 실제 정류장의 좌표를 저장·복원한다. 좌표가 없으면 예시 정류장 좌표를 대신 사용하지 않는다.
6. 빈 좌표를 0도로 해석하던 문제, 도보 시간과 늦는 시간을 반올림하여 실제보다 여유 있게 표시하던 문제를 수정했다.
7. 서버가 재시작되면 지난 아침 알람을 한꺼번에 실행하던 문제를 수정했다. 예정 시각에서 90초를 초과한 알람은 건너뛴다. 건너뛴 알람을 실제 발송 횟수로 세지 않는다.
8. 알람 서버가 브라우저에 저장된 도착정보에만 의존하지 않도록 했다. 알람 시간대에는 등록 노선을 직접 조회하며, 조회 실패 시 임의 시간 대신 정보 없음 안내를 사용한다. 이 동작에는 계속 실행되는 서버와 공급자 API 설정이 필요하다.
9. 알람이 예약 시각보다 조금 늦게 처리될 때도 최신 조회 시점에 맞춰 도착 시간을 계산한다.
10. 오늘 끄기 또는 알람 시간 종료 시 활성 알람과 남은 전송 대기를 정리한다. 휴대폰에는 '출발했어요 - 오늘 알람 종료' 및 오늘 다시 켜기 버튼을 추가했다.
11. 휴대폰 보조 알람을 무조건 매일 반복하던 방식에서, 향후 30일의 선택 요일·공휴일·오늘 끄기를 반영한 개별 예약으로 바꿨다. 앱이 설정을 다시 읽을 때 갱신하고 로그아웃 시 취소한다. 이것은 시작 시각 보조 알람이며 하루의 모든 반복 간격을 오프라인 예약하는 기능은 아니다.
12. 모든 사용자 지정 요일을 해제하면 월요일이 다시 선택되던 문제를 수정했다. 공식 달력의 비공휴일 항목은 휴일 제외 대상으로 사용하지 않는다.
13. 설정한 음성 속도, 진동 패턴 길이, 브라우저 알림 소리 크기가 알림 명세 또는 재생에 반영되지 않던 부분을 연결했다. 웹 진동은 기기·브라우저 지원 범위의 제한이 있으며 실제 진동 세기 보장은 별개다.
14. 여성 음성 선택이 남성 선택 분기로 들어가는 문제, 정보가 없는데 음성 예시에 null 또는 '-분'을 넣는 문제, 여유 있는 버스에도 항상 MUST CATCH를 표시하는 문제를 수정했다.
15. 새 비밀번호가 유효하지 않은데 재설정 링크부터 소진되는 문제를 수정했다.
16. 좁은 가입 화면에서 입력칸 두 개를 가로로 배치하던 부분을 세로 배치로 바꿨다. 브라우저 실제 화면 재검증은 이번 자동 검사에 포함하지 않았다.

## 아직 완료로 볼 수 없는 항목

| 우선순위 | 항목 | 확인된 상태와 남은 일 |
|---|---|---|
| 최우선 | 운영 서버와 영구 저장소 | 현재 서버는 `data/` 파일 저장 및 프로세스 내부 15초 타이머 방식이다. 현재 Vercel Functions 배포만으로 영구 저장과 상시 알람 실행을 보장하는 구성이 아니다. 영구 DB 및 별도 예약 실행 구조, 또는 영구 볼륨을 갖춘 상시 서버로 배포 구조를 정해야 한다. |
| 높음 | 잠금·백그라운드 반복 한국어 TTS | 현재 반복 음성은 주로 브라우저/앱 전면 재생·미리보기다. 앱 종료·잠금 상태에서 끝까지 반복하는 완성된 네이티브 알람 서비스로 볼 수 없다. 단순 API 키 입력만으로 해결되지 않는다. |
| 높음 | 정확한 전체 반복 로컬 예약 | 보조 알람은 30일치 시작 시각 예약이다. 서버와 통신 없이 매 반복 간격의 실시간 버스 판정까지 실행하는 기능은 구현 완료가 아니다. 다른 기기에서 바꾼 설정이 이미 예약된 휴대폰에 즉시 반영되는 동기화도 별도 확인이 필요하다. |
| 높음 | 전체 화면·방해금지 우회 | 권한 요청/알림 코드와 실제 권한 허용은 다르다. Android 잠금·절전·강제 종료·권한 거부 조건 및 iPhone 제한을 실기기로 확인해야 한다. |
| 높음 | 여러 실제 노선 동시 비교 | 선택 목록은 있지만 실제 알람 계산은 주 노선 한 개 중심이다. 여러 노선의 실시간 자료를 동시에 비교하는 기획이라면 추가 구현이 필요하다. |
| 중간 | 모바일 지도 및 일반 사용자 화면 | 웹에는 Kakao 지도 연결 코드가 있다. Flutter 화면은 운영 점검용 대시보드이며 웹 시안과 같은 소비자용 화면 또는 지도 SDK 위젯이 완성된 상태가 아니다. |
| 중간 | APNs·Web Push·계정 메일 | 관련 서버 코드가 존재해도 운영 자격 증명, 발신 도메인, 실제 전달·계정 복구 시험은 이번에 검증하지 않았다. 코드 존재를 운영 완료와 혼동하면 안 된다. |
| 중간 | 자정을 넘는 일정 | 현재 알람 계획은 당일 시작 시각 중심이다. 심야부터 다음 날 새벽까지의 일정은 날짜 경계 요구사항을 확정하고 추가 수정·검증해야 한다. |

Vercel 공식 근거: [함수 실행시간 제한](https://vercel.com/docs/functions/limitations), [로컬과 배포 환경의 파일 저장 차이](https://vercel.com/kb/guide/why-does-my-serverless-function-work-locally-but-not-when-deployed). 현재 코드와 이 제약을 대조한 운영 적합성 판단이다. 운영 계정의 실제 설정 화면을 확인한 결과는 아니다.

## 검증

- 웹·서버 자동 테스트: 기존 242개에서 회귀 테스트를 추가하여 260개 통과
- Flutter 자동 테스트: 8개 통과
- Flutter 정적 검사: 문제 없음
- 웹 진입 코드와 서버 구문 검사 통과
- 실제 Android/iPhone 잠금 상태, 실제 메일·외부 버스 API·푸시 발송, Vercel 재배포 시험은 미실시

테스트 통과는 검사한 시나리오에서의 확인이며, 전체 서비스에 오류가 없거나 원본 기획서를 100% 충족한다는 뜻은 아니다.
