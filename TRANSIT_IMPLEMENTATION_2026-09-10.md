# 대중교통 지각 방지 핵심 구현 현황

## 확정한 기준

- 집에서 정류장·역까지 이동하는 시간은 계산하지 않는다.
- 사용자가 선택한 탑승 지점, 목적지, 도착 목표 시간을 사용한다.
- 선택한 노선의 조회된 도착편을 모두 비교해, 정시 도착 가능한 마지막 교통편의 남은 시간을 표시한다.
- 그 다음 교통편의 예상 도착이 늦을 때만 ‘놓치면 늦는 차’로 표시한다.
- 다음 차 정보가 없으면 마지막 기회라고 단정하지 않는다. 첫 차도 늦으면 별도로 안내한다.
- 모든 결과는 선택한 경로의 예상시간 기준이며, 실제 도착 보장이 아니다.

## 이번에 구현한 부분

1. 웹·서버 공통 계산에서 집→탑승 지점 도보시간 제거.
2. 조회된 모든 도착편 비교와 대상 차량 카운트다운. 알림 문구와 서버 알림도 같은 대상을 사용.
3. 카카오 공식 `GET /v2/routing/publictraffic` 서버 연결 및 응답 정규화.
4. 첫 탑승 이전 도보를 제외하고, 이후 대중교통·환승 도보·목적지까지 도보 구간의 예상시간을 합산.
5. 웹과 Flutter 설정에 경로 조회·선택 추가. 정류장명, 버스 번호, 공식 도착정보 연결을 확인하고 사용자가 탑승 방향을 확인해야 선택 가능.
6. 선택 경로 저장. 목적지·탑승 지점·실시간 연결이 바뀌면 이전 경로는 판단에 사용하지 않음.
7. 경로 시간은 5분 이후 다시 조회, 15분을 넘은 결과는 사용하지 않음. 도착정보는 120초 후 사용 중단.
8. 서버 알림 시간대의 독립적인 경로·버스 도착정보 갱신. 웹 홈이 열려 있을 때도 실시간 도착정보 갱신.
9. 실제 경로 정보가 없는 운영 모드에서는 과거에 저장된 임의 탑승시간으로 지각 여부를 계산하지 않음.

## 아직 완료하지 않은 부분

- 지하철의 실제 도착정보 연결과 호선·방향·급행·종착역 필터. 현재 지하철 경로는 조회 결과로 표시될 수 있으나 알람용 선택을 막아 둠.
- 여러 버스 노선과 지하철을 동시에 비교한 마지막 기회 탐색. 현재는 사용자가 선택한 한 경로 내부 비교.
- 실제 카카오 응답과 공식 버스 도착정보를 결합한 실환경 검증. 자동 검사는 제공처 계약에 맞춘 테스트 응답을 사용함.
- 환승 열차·후속 버스의 실시간 지연까지 반영한 전체 여정 시간 계산. 현재 카카오 구간 예상시간과 첫 버스의 실시간 도착정보를 결합함.
- 휴대폰 종료·절전·방해금지 상태에서 반복 음성 알림을 보장하는 실기기 검증 및 OS별 제약 해결.
- Vercel 환경에서 상시 알림 실행·영속 저장을 보장하는 운영 구조. 기존 단일 프로세스 알림 worker가 계속 실행된다고 가정해서는 안 됨.
- 날짜를 넘는 이동과 알람 구간에 대한 완전한 지원.

## API 설정과 검증 방법

카카오 개발자 콘솔에서 카카오맵 사용 설정을 켜고 REST API 키를 서버 환경변수 `KAKAO_TRANSIT_REST_API_KEY`에 저장한다. 기존 `KAKAO_LOCAL_REST_API_KEY`도 대체 키로 읽는다. 키가 있다는 표시가 API 권한 검증 완료를 의미하지는 않는다. 브라우저용 JavaScript 키와는 별개다.

공식 문서: https://developers.kakao.com/docs/ko/kakaomap/rest-api

경로 설정 순서: 목적지 선택 → 공식 정류장 검색·선택 → 공식 버스 노선 선택 → 대중교통 경로 조회 → 표시된 탑승 지점·방향 확인 → 경로 선택·저장 → 실시간 도착정보와 알림 확인.

카카오 응답의 `totalTime`을 실시간 첫 차 대기시간에 그대로 더하지 않는다. 초기 도보·대기와 중복될 수 있기 때문이다. 현재는 첫 탑승부터의 구간 시간 합계를 사용하며, 환승 대기와 운행 지연을 보장하지 않는 예상치임을 표시한다.

## 검증 상태

- 새 핵심·연결 자동 검사 15개 통과.
- 전체 Node 자동 검사 275개 통과.
- Flutter 정적 검사 오류 없음, 기존 테스트 8개 통과.
- 실제 제공처 키를 이용한 호출, 실기기 화면·알람 시험, 운영 배포는 이번 작업에서 수행하지 않았다.
- GitHub 업로드 및 Vercel 재배포는 수행하지 않았다.
